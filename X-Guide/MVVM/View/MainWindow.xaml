<Window x:Class="X_Guide.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:X_Guide.MVVM"
        xmlns:hc="https://handyorg.github.io/handycontrol"
       
        xmlns:views="clr-namespace:X_Guide.MVVM.View"
        xmlns:vms="clr-namespace:X_Guide.MVVM.ViewModel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="X-Guide"         
        Height="800" Width="1280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip" 
        WindowStyle="None"
        AllowsTransparency="True"
        WindowState="Normal"
        Foreground="White">

    <Grid Background="#1c1c1c">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>


        <DockPanel x:Name="TitleBar" Grid.ColumnSpan="3" Grid.Row="0" Background="#1c1c1c" MouseLeftButtonDown="OnMouseLeftButtonDown">
            <Button x:Name="CloseButton"
                    Click="CloseButton_Click"  Style="{DynamicResource MaterialDesignIconForegroundButton}" 
                    DockPanel.Dock="Right" >
                <materialDesign:PackIcon Kind="WindowClose"></materialDesign:PackIcon>

            </Button>
            <Button x:Name="MaxButton" 
                    Click="MaximizeButton_Click"  Style="{DynamicResource MaterialDesignIconForegroundButton}" 
                    DockPanel.Dock="Right" >
                <materialDesign:PackIcon Kind="WindowMaximize"></materialDesign:PackIcon>
            </Button>
            <Button x:Name="MinButton" Style="{DynamicResource MaterialDesignIconForegroundButton}" 
                    Click="MinimizeButton_Click"
                    DockPanel.Dock="Right">
                <materialDesign:PackIcon Kind="WindowMinimize"></materialDesign:PackIcon>

            </Button>
            <TextBlock  HorizontalAlignment="Center" Margin="5" Foreground="White" Text="Xlent X-Guide version 1.0" Style="{DynamicResource MaterialDesignHeadline6TextBlock}"/>

        </DockPanel>
        <Grid Grid.Row="1" Grid.Column="1" Panel.ZIndex="1"  Background="#1c1c1c">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto">
                </ColumnDefinition>

                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="41*"/>
            </Grid.RowDefinitions>
            <Button Click="Button_Click" Grid.Column="0" Grid.Row="0" MinHeight="44" Style="{DynamicResource MaterialDesignFlatButton}" >
                <materialDesign:PackIcon Kind="HamburgerMenu" Width="24" Height="24" VerticalAlignment="Center"/>
            </Button>
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TextTitle" Text="X-Guide" VerticalAlignment="Center" FontSize="20"  TextAlignment="Center" Background="#1c1c1c" Foreground="White"/>


            <hc:SideMenu Grid.Row="1" Grid.ColumnSpan="2" x:Name="SideMenu" AutoSelect="True" HorizontalAlignment="Left" MinHeight="400" Width="180">
                <hc:Interaction.Triggers>
                    <!--<hc:EventTrigger EventName="SelectionChanged">
                        <hc:EventToCommand Command="{Binding SwitchItemCmd}" PassEventArgsToCommand="False" />
                    </hc:EventTrigger>-->
                </hc:Interaction.Triggers>
                <hc:SideMenuItem Header="Production">
                    <hc:SideMenuItem.Icon>
                        <materialDesign:PackIcon Kind="SitemapOutline" Width="24" Height="24"/>
                    </hc:SideMenuItem.Icon>
                    <hc:SideMenuItem Header="Base Image" Command="{Binding NavigateCommand}" CommandParameter="{x:Static local:PageName.Production}">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="ImageFilterCentreFocusWeak" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Dashboards" Command="{Binding NavigateCommand}" CommandParameter="{x:Static local:PageName.Undefined}">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="MarkerCancel" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Wiki" Command="{Binding NavigateCommand}" CommandParameter="{x:Static local:PageName.Undefined}">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="Pokeball" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                </hc:SideMenuItem>
                <hc:SideMenuItem Header="Engineering">
                    <hc:SideMenuItem.Icon>
                        <materialDesign:PackIcon Kind="CalculatorVariantOutline" Width="24" Height="24"/>

                    </hc:SideMenuItem.Icon>
                    <hc:SideMenuItem Header="Work Items">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Boards">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Backlogs">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Sprints">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Queries">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                </hc:SideMenuItem>
                <hc:SideMenuItem Header="Security">
                    <hc:SideMenuItem.Icon>
                        <materialDesign:PackIcon Kind="SecureOutline" Width="24" Height="24"/>

                    </hc:SideMenuItem.Icon>
                    <hc:SideMenuItem Header="User Admin" ToolTip="Manage user accounts, create new users, and remove existing ones" >
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="AccountMultipleOutline" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Pemission">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="License" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Login" Command="{Binding NavigateCommand}" CommandParameter="{x:Static local:PageName.Login}">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="LoginVariant" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Logout">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="LogoutVariant" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>

                </hc:SideMenuItem>
                <!-- Settings Section -->
                <hc:SideMenuItem Header="Settings">
                    <hc:SideMenuItem.Icon>
                        <materialDesign:PackIcon Kind="SettingsOutline" Width="24" Height="24"/>

                    </hc:SideMenuItem.Icon>
                    <hc:SideMenuItem Header="Setup" Command="{Binding NavigateCommand}" CommandParameter="{x:Static local:PageName.Setting}">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="DatabaseCogOutline" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Releases">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Library">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Task groups">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Header="Deployment groups">
                        <hc:SideMenuItem.Icon>
                            <materialDesign:PackIcon Kind="PokemonGo" Width="24" Height="24"/>

                        </hc:SideMenuItem.Icon>
                    </hc:SideMenuItem>
                </hc:SideMenuItem>

            </hc:SideMenu>

        </Grid>
        <!-- content-->

        <DockPanel x:Name="UserControlContent" Grid.Row="1" Grid.Column="2" >
            <DockPanel.Resources>
                <DataTemplate DataType="{x:Type vms:SettingViewModel}">
                    <views:SettingView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:ProductionViewModel}">
                    <views:ProductionView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:SecurityViewModel}">
                    <views:SecurityView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:EngineeringViewModel}">
                    <views:EngineeringView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:UndefinedViewModel}">
                    <views:UndefinedView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vms:UserLoginViewModel}">
                    <views:UserLoginView/>
                </DataTemplate>


            </DockPanel.Resources>
            <ContentControl Content="{Binding CurrentViewModel}"/>


        </DockPanel>

        <!-- Bottom info bar-->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal"  VerticalAlignment="Center" HorizontalAlignment="Right"  >
 
            <materialDesign:PackIcon  Foreground="{DynamicResource DisconnectedColor}" ToolTip="Disconnected" Kind="LanDisconnect"/>
            <TextBlock  Margin="10 0 10 0" FontWeight="Bold" Foreground="{DynamicResource DisconnectedColor}" >Connection Status :</TextBlock>
            <materialDesign:PackIcon Kind="RobotIndustrial" Foreground="{DynamicResource DisconnectedColor}" />
            <TextBlock  Margin="10 0 15 0" Foreground="{DynamicResource DisconnectedColor}" >Manipulator</TextBlock>
            <materialDesign:PackIcon Kind="MonitorEye" Foreground="{DynamicResource DisconnectedColor}" />
            <TextBlock  Margin="10 0 0 0" Foreground="{DynamicResource DisconnectedColor}" >Vision</TextBlock>
            <TextBlock  Foreground="White" x:Name="DateTimeTextBlock" Margin="50 0 20 0" >Thursday, 23 Feb 2023 9:46 am</TextBlock>
        </StackPanel>


    </Grid>
</Window>
