<UserControl x:Class="X_Guide.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.MVVM.View" 
             xmlns:valid="clr-namespace:X_Guide.Validation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources> 
        <ControlTemplate x:Key="validationTemplate">
            <DockPanel>
                <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(Validation.Errors)[0].ErrorContent}" Foreground="Red" />
            </DockPanel>
        </ControlTemplate>

        <SolidColorBrush x:Key="PrimaryBlueColor" Color="#348EF6"/>
        <SolidColorBrush x:Key="SecondaryBlueColor" Color="#EFF2FF"/>

        <SolidColorBrush x:Key="PrimaryTextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#BBC1D1"/>

        <SolidColorBrush x:Key="PrimaryGrayColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryBlackColor" Color="#000000"/>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBlackColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Margin="0,10,10,10" Grid.ColumnSpan="2">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="12*"/>
                    <ColumnDefinition Width="20*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0" Padding="3"   Text="Machine ID"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Machine Description"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Software Revision"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Robot IP"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Robot Port"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Scanner IP"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Scanner Port"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Shift Start Time"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Max Scanner Capture Time(s)"/>
                    <TextBlock Foreground="{DynamicResource PrimaryGrayColor}" Margin="0 10 0 0"  Padding="3"  Text="Vision Recipe Path"/>

                </StackPanel>

                <StackPanel Grid.Column="1">
                    <!--<TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" 
                             <TextBox.Text>
                        <Binding Path="MachineID" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <local1:Validation/>
                            </Binding.ValidationRules>
                        </Binding>
                        </TextBox.Text>
                        <TextBox/>-->
                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2">
                        <TextBox.Text>
                            <Binding Path="MachineID" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <valid:IPValidation/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    

                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" Text="{Binding MachineDescription}" />
                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" Text="{Binding SoftwareRevision}"/>
                    <Border BorderThickness="1" BorderBrush="#FFABADB3" Background="White" Margin="5" Padding="3">
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="txtRobotIP1" Tag="txtRobotIP2" Width="30" Margin="0"  Style="{DynamicResource TextBoxStyle}"  TextChanged="TxtIPLength_TextChanged" PreviewKeyDown="txtRobotIP1_PreviewKeyDown" PreviewTextInput="TxtIPAddress_PreviewTextInput" />
                        <TextBlock Text="." Margin="0,0,5,0" />
                            <TextBox x:Name="txtRobotIP2" Tag="txtRobotIP3" Style="{DynamicResource TextBoxStyle}" TextChanged="TxtIPLength_TextChanged" PreviewTextInput="TxtIPAddress_PreviewTextInput" PreviewKeyUp="txtRobotIP2_PreviewKeyUp" Width="30" Margin="0" />
                        <TextBlock Text="." Margin="0,0,5,0" />
                            <TextBox x:Name="txtRobotIP3"  Tag="txtRobotIP4" Style="{DynamicResource TextBoxStyle}"  TextChanged="TxtIPLength_TextChanged" PreviewTextInput="TxtIPAddress_PreviewTextInput" PreviewKeyUp="txtRobotIP2_PreviewKeyUp"  Width="30" Margin="0" />
                        <TextBlock Text="." Margin="0,0,5,0" />
                            <TextBox x:Name="txtRobotIP4"  Style="{DynamicResource TextBoxStyle}"  PreviewTextInput="TxtIPAddress_PreviewTextInput"  Width="30" Margin="0" />
                        </StackPanel>
                    </Border>

                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" Text="{Binding RobotPort}"  PreviewTextInput="TxtPort_PreviewTextInput"/>
                    <Border Margin="5" Padding="3">
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="txtScannerIP1"  Tag="txtScannerIP2" Width="30" Margin="0"  Style="{DynamicResource TextBoxStyle}" TextChanged="TxtScannerIPLength_TextChanged" PreviewTextInput="TxtIPAddress_PreviewTextInput"/>
                            <TextBlock Text="." Margin="0,0,5,0"/>
                            <TextBox x:Name="txtScannerIP2"  Tag="txtScannerIP3" Style="{DynamicResource TextBoxStyle}" TextChanged="TxtScannerIPLength_TextChanged" PreviewTextInput="TxtIPAddress_PreviewTextInput" Width="30" Margin="0" />
                            <TextBlock Text="." Margin="0,0,5,0" />
                            <TextBox x:Name="txtScannerIP3"  Tag="txtScannerIP4" Style="{DynamicResource TextBoxStyle}"  TextChanged="TxtScannerIPLength_TextChanged" PreviewTextInput="TxtIPAddress_PreviewTextInput"   Width="30" Margin="0" />
                            <TextBlock Text="." Margin="0,0,5,0" />
                            <TextBox x:Name="txtScannerIP4"  Style="{DynamicResource TextBoxStyle}"  PreviewTextInput="TxtIPAddress_PreviewTextInput"  Width="30" Margin="0" />
                        </StackPanel>
                    </Border>
                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" Text="{Binding VisionPort}"  PreviewTextInput="TxtPort_PreviewTextInput"/>
                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" Text="{Binding ShiftStartTime}"/>
                    <TextBox Foreground="{DynamicResource PrimaryGrayColor}"  Margin="5 5 0 5" Padding="2" Text="{Binding MaxScannerCapTime}"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Foreground="{DynamicResource PrimaryGrayColor}" Width="400" Margin="5 5 0 5" Padding="2" Text="{Binding LogFilePath}"/>
                        <Button x:Name="Browse" Margin="20 0 0 0" Content="Browse"/>
                    </StackPanel>
                    <CheckBox Margin="8 5" Content="Debug Log"/>
                </StackPanel>
            </Grid>
            <Button HorizontalAlignment="Right" Width="50"  Margin="5 10" Command="{Binding SaveCommand}" >Save</Button>
            <Button HorizontalAlignment="Left" Width="50"  Margin="5 10" Command="{Binding NavigateCommand}" >Navigate</Button>
         
        </StackPanel>
    </Grid>
</UserControl>
