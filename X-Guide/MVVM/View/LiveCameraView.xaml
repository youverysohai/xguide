<UserControl x:Class="X_Guide.MVVM.View.LiveCameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:X_Guide.MVVM.View"
             mc:Ignorable="d"
                xmlns:hc="https://handyorg.github.io/handycontrol"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <hc:SimpleStackPanel Orientation="Vertical">
            <ContentControl Height="500" Content="{Binding VisionView}" VerticalAlignment="Top">
            </ContentControl>
            <Button Content="Start" Command="{Binding StartLiveImageCommand}"></Button>
            <Button Content="Stop" Command="{Binding StopLiveImageCommand}"></Button>
        </hc:SimpleStackPanel>
    </Grid>
</UserControl>