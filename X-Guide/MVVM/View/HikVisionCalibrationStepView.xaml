<UserControl x:Class="X_Guide.MVVM.View.HikVisionCalibrationStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.MVVM.View"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">

            <TextBlock  Text="Vision Flow for Calibration" HorizontalAlignment="Center" FontSize="35" Margin="0 0 0 25" />
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">

                <ComboBox
                        ItemsSource="{Binding Procedures}"
                        SelectedItem="{Binding Procedure}"   Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                                DisplayMemberPath="Name"
               SelectedValue="{Binding Procedure}"
                    SelectedValuePath="Name"
                              Width="300" IsEnabled="{Binding IsProcedureEditable}"
                              materialDesign:HintAssist.Hint="Select Calibration Procedure"
                                                      HorizontalAlignment="Left"
                              Margin="0">
                </ComboBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
