<UserControl x:Class="X_Guide.MVVM.View.CalibrationWizardSteps.Step2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.MVVM.View.CalibrationWizardSteps"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
                       xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <Grid>
        <GroupBox Style="{DynamicResource MaterialDesignGroupBox}"  Margin="20">
            <GroupBox.Header>
                <TextBlock Foreground="{StaticResource PrimaryBlackColor}" TextAlignment="Center" FontWeight="DemiBold" FontSize="36"  Text="Select Manipulator"/>
            </GroupBox.Header>
            <StackPanel  Margin="60 10 60 10" >
                <ComboBox Margin="50 50 50 0" FontSize="20" Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                    materialDesign:HintAssist.Hint="Manipulator Name"
                          >
                    <ComboBoxItem>1</ComboBoxItem>
                    <ComboBoxItem>2</ComboBoxItem>
                    <ComboBoxItem>3</ComboBoxItem>
                </ComboBox>
                <StackPanel Margin="10 0 50 0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock FontSize="16" VerticalAlignment="Center" Text="New Manipulator?"/>
                    <Button Style="{DynamicResource MaterialDesignFlatButton}" Padding="5"  Margin="5">Create New</Button>
                </StackPanel>
                <TextBox Margin="50" Text="Gantry System" IsReadOnly="True" FontSize="20"  Style="{DynamicResource MaterialDesignFilledTextBox }"
                         materialDesign:HintAssist.Hint="Selected Manipulator's Type" TextAlignment="Center"/>
                <StackPanel Margin="10 0 50 0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{DynamicResource MaterialDesignFlatButton}" Padding="5"  Margin="5">Cancel</Button>
                    <Button Style="{DynamicResource MaterialDesignOutlinedButton}" Padding="5"  Margin="5">Apply</Button>

                </StackPanel>

            </StackPanel>

        </GroupBox>
    </Grid>
</UserControl>
