<UserControl x:Class="X_Guide.MVVM.View.CalibrationWizardSteps.Step5"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.MVVM.View.CalibrationWizardSteps"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="0 0 0 10">
        <materialDesign:Card UniformCornerRadius="20" Margin="0 10 15 0">

            <StackPanel Orientation="Vertical" VerticalAlignment="Center" >
                <Label Content="Based on Step 1 and Step 2, you have selected..." HorizontalAlignment="Center" Style="{DynamicResource MaterialDesignLabel}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                    <Label Content="Name:" Style="{DynamicResource MaterialDesignLabel}"/>
                    <TextBlock Style="{StaticResource ReadOnlyTextBlock}" Text="Gantry 1"/>
                    <Label Content="Type:" Style="{DynamicResource MaterialDesignLabel}"/>
                    <TextBlock Style="{StaticResource ReadOnlyTextBlock}" Text="Gantry System"/>
                    <Label Content="Orientation:" Style="{DynamicResource MaterialDesignLabel}"/>
                    <TextBlock Style="{StaticResource ReadOnlyTextBlock}" Text="Look Upward"/>
                </StackPanel>
                <TextBlock Text="Jog Robot To Desired Calibration Point" HorizontalAlignment="Center" FontSize="35" Margin="0 0 0 25"/>
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                     <!-- Live Image and overlay border-->
                    <Grid>
                        <Image Source="../../../Style/ImageSource/mtg.jpg" Stretch="UniformToFill" />
                        <Grid Background="#50000000">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource ConnectedColor}" BorderThickness="2"></Border>
                        </Grid>
                    </Grid>
                    
                </Grid>



            </StackPanel>
        </materialDesign:Card>
    </Grid>


</UserControl>
