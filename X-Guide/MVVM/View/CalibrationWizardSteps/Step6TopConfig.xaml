<UserControl x:Class="X_Guide.MVVM.View.CalibrationWizardSteps.Step6TopConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:X_Guide.MVVM.View.CalibrationWizardSteps"
             xmlns:custLayout ="clr-namespace:X_Guide.CustomControls.Layouts"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!--<ContentControl Grid.Column="0" Content="{Binding VisionView}">
        </ContentControl>-->
        <custLayout:CalibrationResultLayout CalibrationData="{Binding Calibration.CalibrationData}" Grid.Column="1" Margin="10 0" SaveCalibrationCommand="{Binding SaveCalibrationCommand}" />

        <ContentControl Content="{Binding NinePoint}" Width="500"></ContentControl>
        <Button Command="{Binding StartVision9PointCommand}" VerticalAlignment="Bottom">Start 9 Point!!!</Button>
        <Button Command="{Binding StartCalibrationCommand}" VerticalAlignment="Bottom" HorizontalAlignment="Right">Start Calib</Button>
    </Grid>
</UserControl>