<UserControl x:Class="X_Guide.CustomControls.CustomLoading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.CustomControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
       
    </UserControl.Resources>
    <Grid>
        <Button     x:Name="OpenDialogBtn" Visibility="Collapsed"
                                Width="128"
                               Command="{x:Static materialDesign:DialogHost.OpenDialogCommand}"
                                >
        </Button>
        <Button     x:Name="CloseDialogBtn" Visibility="Collapsed"
                                Width="128"
                               Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                                >
        </Button>
    </Grid>
</UserControl>
