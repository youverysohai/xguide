<UserControl x:Class="X_Guide.CustomControls.Layouts.NineBoxCalibLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.CustomControls" xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:enums="clr-namespace:X_Guide.Enums"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            
        </Style>
        <Style TargetType="Border"> 
            <Setter Property="BorderThickness" Value="3"/>
        </Style>
    </UserControl.Resources>

    <GroupBox  Grid.Column="1" Visibility="Visible" Grid.Row="2" Style="{StaticResource GroupBoxTab}" MinHeight="210">
        <i:Interaction.Triggers>
            <i:DataTrigger Binding="{Binding}" Value="{x:Static enums:Orientation.LookDownward}">
                <i:ChangePropertyAction PropertyName="Visibility" Value="Visible" />
            </i:DataTrigger>
        </i:Interaction.Triggers>
        <GroupBox.Header>
            <TextBlock Text="Top Config Calibration" Style="{StaticResource SubtitleTextBlockStyle}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
        </GroupBox.Header>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <UniformGrid Grid.Row="0" Rows="3" Columns="3">
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}" Grid.Row="1" Grid.Column="1">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[0]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="1" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[1]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="2" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[2]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="3" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[3]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="4" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[4]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="5" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[5]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="6" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[6]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="7" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[7]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="8" />
                </Border>
                <Border BorderBrush="{DynamicResource PrimaryBlueColor}">
                    <i:Interaction.Triggers>
                        <i:DataTrigger Binding="{Binding NinePointState[8]}" Value="True">
                            <i:ChangePropertyAction PropertyName="Background" Value="{DynamicResource PrimaryBlueColor}"></i:ChangePropertyAction>
                        </i:DataTrigger>
                    </i:Interaction.Triggers>
                    <TextBlock Text="9" />
                </Border>
            </UniformGrid>
            <Label Content="Instruction: Move End Effector to box accordingly" Style="{DynamicResource DefaultLabelStyle}" Grid.Row="1" HorizontalAlignment="Center"></Label>
            <ui:SimpleStackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
                <Button Content="Start" Margin="5"/>
                <Button Content="Next" Margin="5"/>
                <Button Content="Cancel"  Margin="5"/>
            </ui:SimpleStackPanel>
        </Grid>
    </GroupBox>
</UserControl>
