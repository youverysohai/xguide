<UserControl x:Class="X_Guide.CustomControls.Layouts.CameraLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:X_Guide.CustomControls.Layouts"
             mc:Ignorable="d" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:Release ="clr-namespace:VMControls.WPF.Release;assembly=VMControls.WPF.Release"
             d:DesignHeight="450" d:DesignWidth="800">
    <ui:PopupEx  VerticalOffset="1000" HorizontalOffset="200"  x:Name="StandardPopup">
        <Canvas x:Name="ContentCanvas">
            <Border BorderBrush="{DynamicResource ConnectedColor}" 
                Background="{StaticResource ApplicationPageBackgroundThemeBrush}"
                BorderThickness="2" Width="800" Height="600">
                <StackPanel Orientation="Vertical"  HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Release:VmRenderControl Width="800" Height="500"/>
                    <Button Content="Close" Click="ClosePopupClicked" Margin="0 0 10 0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Canvas>
    </ui:PopupEx>
</UserControl>
